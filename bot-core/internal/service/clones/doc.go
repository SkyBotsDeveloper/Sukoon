// Package clones is reserved for clone-management workflows.
// Bot identity and webhook isolation already exist at the schema and
// runtime boundary through bot_instances and explicit webhook routing.
package clones
