// Package federation is reserved for the v2 federation runtime.
// The canonical schema exists in migrations, but implementation is
// intentionally deferred until the core moderation stack is stable.
package federation
